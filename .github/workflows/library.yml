name: Test library

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  test-and-lint:
    uses: yeencloud/dpl-ci/.github/workflows/test.yml@main